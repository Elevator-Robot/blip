service: aws-python-project

frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.9

functions:
  url:
    handler: handler.hello
    events:
      - http:
          path: hello
          method: post
